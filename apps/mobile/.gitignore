# Expo / RN
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# Env / secrets
.env
.env.local

# EAS
.eas/

# Expo CNG — native projects are regenerated by `expo prebuild`
# (scripts/build_harness_apk.sh re-runs prebuild + applies arm64/worker flags)
/android/
/ios/

# Build outputs (APK lives on prod /static/internal/, not in git)
*.apk
*.aab
