#!/bin/sh
# One-line installation is deliberately disabled in the source preview.
# A complete signed bundle and published immutable release are required first.

printf '%s\n' 'awp-vm-agent one-line installation is unsupported until a signed release bundle is published' >&2
exit 77
