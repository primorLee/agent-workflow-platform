apiVersion: 1

providers:
  - name: Agent Workflow Platform
    orgId: 1
    folder: Agent Workflow Platform
    folderUid: awp-folder
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards